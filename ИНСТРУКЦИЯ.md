# Телеграм-бот для конкурса - Полная инструкция

## Что это?

Бот для проведения конкурсов в Telegram, который:
- Принимает от участников минимум 3 ссылки на приглашённых людей
- Сохраняет все данные в приватный канал (это и есть база данных!)
- Работает 24/7 на бесплатном хостинге Render.com

---

## Шаг 1: Создание бота в Telegram

### 1.1. Создай бота

1. Открой Telegram и найди **@BotFather**
2. Отправь команду `/newbot`
3. Придумай имя бота (например: `Мой конкурс бот`)
4. Придумай username (например: `my_contest_bot`)
5. **СОХРАНИ ТОКЕН!** Он выглядит так: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`

### 1.2. Создай приватный канал (база данных)

1. Создай новый канал в Telegram
2. Назови его (например: `Логи конкурса`)
3. **Сделай канал ПРИВАТНЫМ!**
4. Добавь своего бота в канал как **администратора**:
   - Настройки канала → Администраторы → Добавить администратора
   - Найди своего бота и добавь

### 1.3. Узнай ID канала

**Способ 1 (простой):**
1. Перешли любое сообщение из канала боту **@userinfobot**
2. Он покажет ID канала (например: `-1001234567890`)

**Способ 2 (через веб):**
1. Открой канал в веб-версии Telegram (web.telegram.org)
2. В адресной строке увидишь число после `/channel/` — это ID
3. Добавь `-100` в начало (например: `-1001234567890`)

**СОХРАНИ ID КАНАЛА!**

---

## Шаг 2: Подготовка кода

### 2.1. У тебя уже есть все файлы в папке `telegram-contest-bot`:

```
telegram-contest-bot/
├── bot.py              ← Основной код бота
├── requirements.txt    ← Зависимости Python
├── .env.example        ← Пример настроек
├── .gitignore          ← Что не загружать в Git
├── render.yaml         ← Конфигурация для Render
└── ИНСТРУКЦИЯ.md      ← Этот файл
```

### 2.2. Не нужно ничего менять в коде!

Все настройки будем указывать на Render.com.

---

## Шаг 3: Загрузка на GitHub

### 3.1. Создай репозиторий на GitHub

1. Зайди на **github.com** и войди в аккаунт
2. Нажми **New repository** (зелёная кнопка)
3. Название: `telegram-contest-bot`
4. Сделай репозиторий **Приватным** (Private)
5. Нажми **Create repository**

### 3.2. Загрузи код

Открой командную строку в папке `telegram-contest-bot` и выполни:

```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ТВОЙ_USERNAME/telegram-contest-bot.git
git push -u origin main
```

**Замени `ТВОЙ_USERNAME` на свой юзернейм GitHub!**

---

## Шаг 4: Деплой на Render.com (БЕСПЛАТНО!)

### 4.1. Регистрация

1. Зайди на **render.com**
2. Нажми **Get Started** или **Sign Up**
3. Зарегистрируйся через GitHub (это быстрее)

### 4.2. Создание сервиса

1. На главной странице Render нажми **New +** → **Web Service**
2. Подключи свой GitHub репозиторий:
   - Нажми **Connect account** (если первый раз)
   - Найди репозиторий `telegram-contest-bot`
   - Нажми **Connect**

### 4.3. Настройка сервиса

**Заполни поля:**

| Поле | Значение |
|------|----------|
| **Name** | `telegram-contest-bot` (любое имя) |
| **Region** | Выбери ближайший регион |
| **Branch** | `main` |
| **Runtime** | `Python 3` |
| **Build Command** | `pip install -r requirements.txt` |
| **Start Command** | `python bot.py` |
| **Instance Type** | **Free** (бесплатный!) |

### 4.4. Добавь переменные окружения

Прокрути вниз до раздела **Environment Variables** и добавь:

| Key | Value |
|-----|-------|
| `BOT_TOKEN` | Твой токен от @BotFather |
| `LOG_CHANNEL_ID` | ID твоего канала (с минусом!) |

**Пример:**
```
BOT_TOKEN = 1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
LOG_CHANNEL_ID = -1001234567890
```

### 4.5. Запуск

1. Нажми **Create Web Service** внизу страницы
2. Render начнёт деплой (займёт 2-3 минуты)
3. Дождись статуса **Live** (зелёная галочка)

---

## Шаг 5: Проверка работы

### 5.1. Проверь логи на Render

1. На странице сервиса перейди во вкладку **Logs**
2. Должно быть:
   ```
   🤖 Бот запущен!
   📝 Логи отправляются в канал: -1001234567890
   ```

### 5.2. Протестируй бота

1. Найди своего бота в Telegram
2. Отправь `/start`
3. Пришли 3 ссылки на профили (например: `t.me/durov`)
4. Нажми кнопку "Выполнил условия"

### 5.3. Проверь канал

1. Открой свой приватный канал
2. Там должны появиться сообщения с логами

---

## Команды бота

- `/start` — Начать участие в конкурсе
- `/stats` — Статистика (сколько участников, ссылок)

---

## Как работает бот

### Для участника:

1. Пользователь пишет `/start`
2. Отправляет минимум 3 ссылки: `t.me/username`
3. Нажимает кнопку "Выполнил условия"
4. Получает подтверждение участия

### Для тебя (админа):

- Все ссылки приходят в приватный канал в реальном времени
- Каждая ссылка — отдельное сообщение
- Финальное сообщение — полная заявка участника
- Используй `/stats` в боте для статистики

---

## Важные моменты

### Бесплатный тариф Render:

- Бот засыпает после 15 минут неактивности
- Просыпается за 30-60 секунд при первом сообщении
- Для участников это незаметно!
- **Ограничение:** 750 часов в месяц (хватит!)

### Безопасность:

- Токен бота и ID канала НЕ в коде, а в переменных окружения
- Канал приватный — никто не увидит данные
- Репозиторий приватный — код не публичный

---

## Что делать, если что-то не работает

### Бот не отвечает:

1. Проверь логи на Render (вкладка Logs)
2. Убедись, что статус сервиса **Live**
3. Проверь переменные окружения (токен и ID канала)

### Бот не отправляет в канал:

1. Убедись, что бот добавлен как **администратор** канала
2. Проверь, что ID канала правильный (с минусом!)
3. Канал должен быть **приватным**, а не публичным

### Render пишет ошибку:

1. Проверь логи: вкладка **Logs**
2. Убедись, что файл `requirements.txt` загружен
3. Попробуй **Manual Deploy** (кнопка вверху справа)

---

## Обновление бота

Если нужно изменить код:

1. Отредактируй файлы локально
2. Загрузи на GitHub:
   ```bash
   git add .
   git commit -m "Обновление"
   git push
   ```
3. Render автоматически задеплоит изменения!

---

## Дополнительные возможности

### Изменить текст сообщений:

Открой `bot.py` и найди текст в кавычках. Например:

```python
await message.answer(
    "👋 <b>Привет!</b>\n\n"
    "Твой текст здесь..."
)
```

### Изменить количество ссылок:

Найди в коде `>= 3` и замени на нужное число.

### Добавить дату конкурса:

В коде уже указана дата: `25 ноября 2025`. Найди и измени.

---

## Контакты и поддержка

Если возникли проблемы:

1. Проверь логи на Render
2. Убедись, что все шаги выполнены правильно
3. Перечитай инструкцию с начала

---

## Готово! 🎉

Теперь у тебя есть:
- ✅ Работающий бот в Telegram
- ✅ База данных в приватном канале
- ✅ Бесплатный хостинг 24/7
- ✅ Автоматический сбор заявок

**Удачи с конкурсом!** 🚀
